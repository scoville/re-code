{
  "name": "re-code",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "bsb -clean-world && rm -fr lib",
    "build": "bsb -make-world",
    "test": "yarn build && node lib/js/tests/Test.bs.js"
  },
  "devDependencies": {
    "bs-platform": "^9.0.0"
  }
}
