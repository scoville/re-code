{
  "name": "re-code",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rescript clean",
    "build": "rescript build",
    "test": "yarn build && node lib/js/tests/DecodeTest.bs.js && node lib/js/tests/EncodeTest.bs.js && echo All tests passed"
  },
  "devDependencies": {
    "rescript": "^9.1.4"
  }
}
